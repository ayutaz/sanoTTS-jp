# β の A/B 聴取（40 試行）

> ## ⚠️ **決着済み（D-038）。追加の聴取は不要。**
> **β = 0 で確定**し、式7（摩擦音へのノイズ注入）は**採用しない**（M-60）。
> ⚠️ **聴取者は 1 名**なので、別の人に聴いてもらう価値はある。その場合は下記の手順で。
> ⚠️ 上流（英語）は β=6.0 を採用している（`docs/upstream-sanotts.md`）。

`pairs/T000_A.wav` と `pairs/T000_B.wav` を聴き比べて、
**好ましいほう**を `answer_sheet.csv` に記入してください。

- 聴きどころ: **サ行・シャ行・ツ・チ・「です」「ます」「した」の無声化**が
  自然か、シューという笛のような音（whistly）になっていないか
- どちらとも言えなければ「同じ」で構いません
- **同じ文が 2 回出てきます**（順序と左右はランダム）。
  同じ答えになるかで一貫性を測るので、前の答えは気にせず素直に選んでください
- ⚠️ `key.json` は**先に見ないでください**（どちらが β=0 か書いてあります）

採点: `uv run python scripts/score_listening.py --dir reports/listening_beta`
