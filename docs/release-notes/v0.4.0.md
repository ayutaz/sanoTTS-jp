# v0.4.0 — 蒸留テキストを CC0 / パブリックドメインのみにした

2026-09-10。**この版の理由はライセンスであって、品質ではありません。**

蒸留に使うテキストから **JSUT ver1.1（CC-BY-SA-4.0 = 唯一の継承付き素材）6,380 行を外し**、
**CC0 / パブリックドメインのみの 14,513 行**で学習し直した重み **v4** を配ります。
これで**蒸留テキスト由来の継承（share-alike）リスクが消えます**。

⚠️ **出力の用途制限とコピーレフトは 1 つも減りません。** それは教師の fine-tune に
使った**つくよみちゃんコーパス 100 発話**から来ており、2026-09-10 に
**声はつくよみちゃんのままにすると決めました**（`docs/decisions.md` D-058）。
**したがってこの 2 つは今後の版でも残ります** — 「将来の版で消える」ものとして扱わないでください。

⚠️ **v4 の音は誰も聴いていません。実機にも載せていません**（QEMU まで）。

---

## どちらを使うべきか

| あなたの状況 | 使うもの |
|---|---|
| **モデルを再配布する / 製品に組み込む** | **v4**。継承リスクが無いのはこちらだけ |
| 手元で試すだけ / 既に v3 で動いている | **v3 のままで構いません**。v3 の資産は今もダウンロードできます |
| **アクセントの正確さが要る** | ⚠️ **v3**。下記のとおり v4 は落ちています |

⚠️ **v3 の資産を消していません。** v3 を使う限り、`LICENSE-MODEL.md` §5 の
継承リスクはそのまま残ります（帰属ブロック (A) の JSUT 行も v3 では必須です）。

---

## 何が変わったか

### 1. 蒸留テキストが CC0 / PD のみになりました

| | v3 | **v4** |
|---|---:|---:|
| 蒸留に使った行数 | 20,893 | **14,513** |
| うち JSUT ver1.1（CC-BY-SA-4.0） | 6,380 | **0** |
| 学習に入った発話 | 20,792 | **14,426** |

論文の 14,343 行を上回る行数を保てました。**音素カバレッジに穴は空いていません** —
出現する音素は **54 種で v3 と同一**、最も稀な 3 音素も同じでした（`docs/measurements.md` M-115）。

判定は 1 か所（`src/saanotts_jp/corpus_license.py`）の allowlist で行い、
**ラベルパックそのものを検査できます**（`scripts/check_corpus_license.py --pack`）。
⚠️ **前方一致ではなく完全一致**で判定します（`cv/europarl-v7-ja` のような
「CC0 のコーパスに見えるが違う」ものを取り込まないため）。

### 2. 品質は SCOREQ も CER も差を検出できませんでした

| 指標 | v3 | **v4** | 判定 |
|---|---:|---:|---|
| SCOREQ synthetic/nr（生徒） | 1.2716 | **1.2552** | |
| SCOREQ 教師比 | 0.6444 [0.6044, 0.6843] | **0.6361** [0.5902, 0.6853] | |
| **SCOREQ v4 − v3（対応あり）** | — | **−0.0164** [−0.0671, +0.0361] | ⚠️ **CI が 0 を跨ぐ = 検出できず** |
| かな CER（生徒） | 0.1671 | **0.1670** | 実質同一 |
| int8 blob 合計 | 624,692 B | **624,692 B** | 完全一致 |

⚠️ **n=24。** 絶対値は日本語で較正されていないので、論文の英語モデルと比べないでください。

### 3. ⚠️ アクセントは落ちました（37/37 → 31/37）

**検出できた差はここだけです。**

| run | 変えたもの | アクセント符号一致 |
|---|---|---:|
| v3 | — | **37/37 = 1.000** |
| **v3_seed2** | **seed だけ**（パックは v3 のまま） | **34/37 = 0.919** |
| **v4** | テキスト（CC0/PD のみ） | **31/37 = 0.838** |

**同一パックで seed だけ変えると 34/37 まで落ちました** = 劣化の**大部分は run 間の
ばらつき**です（`docs/measurements.md` M-117）。v4 が失った 3 ペアは v3_seed2 が
失った 3 ペアを含みます。

⚠️ **テキストの影響を否定はできません。** seed 対照は n=1 run で、
「JSUT を外したこと自体の寄与」は分離できていません。

⚠️ **教師の差が大きいペアでは落ちていません**: `|Δ_T| ≥ 3.5 st` で **17/17 = 1.000**。
落ちたのは教師自身の差が小さいペア（`1型 vs 2型` の cos 平均 0.3824）です。

⚠️ **`magnitude_ratio` は 1.193 → 1.283** に増えました（生徒の起伏が教師より 28% 大きい）。
**過剰強調かどうかは聴かないと分かりません。**

### 4. アクセント指標の欠陥を直しました（測り方の訂正）

**教師ゲートが生徒に依存していました**（`docs/decisions.md` C-076）。
「教師の差が十分大きいペアだけ数える」というゲートが、**生徒の F0 が取れたモーラだけ**の
マスクで計算されていたため、**生徒が悪いとゲートも動いて分母が変わる**状態でした。

直した結果、**3 run すべてで分母が 37/38 にそろいました**（v4 は 36 → 37）。
上の表の数字は**すべて修正後**です。⚠️ **陽性対照つき**で、旧実装だと 5.196 st → 0.000 st と
判定が反転することを CI で毎回確認しています（`scripts/test_accent_gate.py`）。

---

## 配布物

**モデルの重み**（`LicenseRef-sanoTTS-jp-Model-1.0`。`LICENSE-MODEL.md` を必ず読んでください）:

| ファイル | サイズ | SHA-256（先頭 16 桁） | 中身 |
|---|---:|---|---|
| `saanotts-jp-v4-int8.bin` | 654,032 B | `a1eb6b0812e2ad2a` | 端末用の重み。**blob v2** |
| `saanotts-jp-v4-fp32.bin` | 2,249,792 B | `ccbded9f9846a1f2` | ホストで動かす / ゴールデンテスト用 |
| `golden-v4-int8.bin` | 794,240 B | `da360dbe4d541d29` | 参照出力（`make -C csrc int8-golden`） |
| `golden-v4-fp32.bin` | 786,912 B | `1b0a6a6ea29b55fd` | 参照出力（`make -C csrc test`） |
| `saanotts-jp-v4-stage4.pt` | 2,744,874 B | `be5bf3f586cc6de7` | PyTorch のチェックポイント |
| `k1-dict-438750.bin` | 13,702,320 B | （v0.3.0 と同一） | 漢字辞書。**v3 と同じもの** |

⚠️ **`k1-dict-438750.bin` は重みではないので v4 で変わりません。** 焼き直しは要りません。

**焼くだけの firmware**（3 構成すべてに **v4 の重みが入っていることを照合済み**）:

| ファイル | flash | 板 | コンソール | SHA-256（先頭 16 桁） |
|---|---:|---|---|---|
| `m5-cores3-firmware-kanji-16mb.bin` | 16 MB | **M5Stack CoreS3 / スタックチャン** | native USB | `73bb6dc89894d236` |
| `esp32s3-firmware-kanji-16mb-usbjtag.bin` | 16 MB | ESP32-S3 DevKit（16 MB） | native USB | `15efe00c979ea1d3` |
| `esp32s3-firmware-w8a8-pie-usbjtag.bin` | 8 MB | ESP32-S3 DevKit（8 MB）。**かなのみ** | native USB | `30ff7bb3a6be3cdd` |

**v4 が入っていることの照合方法**（推測ではなく抽出して SHA-256 を突き合わせました）:

| イメージ | 見た場所 | 結果 |
|---|---|---|
| 漢字 16 MB / かな 8 MB | `0x210000` から 654,032 B | **`a1eb6b0812e2ad2a…` = v4 と一致** |
| 漢字 16 MB / M5 | `0x2d0000` から 13,702,320 B | **`f162c922074d7681…` = 凍結辞書と一致** |
| M5 | `.rodata`（パーティションを焼かない構成） | **654,032 B が連続で bit 一致**（`0x23c40`） |

### 焼き方

```bash
# ⚠️ 先に元のファームを退避してください
esptool.py --chip esp32s3 -p /dev/cu.usbmodemXXXX read_flash 0 0x1000000 backup.bin

esptool.py --chip esp32s3 -p /dev/cu.usbmodemXXXX write_flash 0x0 m5-cores3-firmware-kanji-16mb.bin
```

⚠️ **`--flash_mode qio` を渡さないでください。** ヘッダが QIO になって ROM ローダが
読めず、ブートループします（`docs/measurements.md` M-86）。**上のコマンドのまま**使ってください。

⚠️ **M5 版は起動すると自分から 1 度喋ります**（スタックチャン用の既定）。
DevKit 版は喋らず `かな> ` のプロンプトを出します。

⚠️ **この 3 イメージは QEMU で動かせません**（**QIO** で焼くため。QEMU の flash モデルは
QIO を受け付けません）。QEMU で通したのは**同じソース・同じ blob の DIO ビルド**で、
違いは**フラッシュモードと起動時発話の 2 点だけ**です
（`docs/measurements.md` M-120 / M-121 / M-122）。**実機で確かめたわけではありません。**

---

## ⚠️ できないこと・分かっていないこと

- **音を聴いていません。** v4 の合成音は 1 秒も聴取していません（G32）。
  v3 は 1 名がざっと聴いて「問題なかった」と報告しています（対照なし・盲検なし）が、
  **v4 についてはそれさえありません。**
- **実機に載せていません。** 検証は QEMU までです。firmware は起動から合成完了まで通り、
  PCM の checksum が 3 構成で一致していますが、**M5.Speaker の実挙動は別です。**
- **アクセントが落ちた原因を分離できていません**（上記 3）。
- **漢字構成の QEMU 初回起動で 1 度だけ core 1 が panic しました**（`LoadProhibited` /
  `EXCVADDR: 0x0`）。3 回追試して再現しませんでしたが、**原因は特定していません**
  （`docs/measurements.md` M-121 §4）。
- **RTF の分母は今も未決**です（満チャンク 1 pull = 0.446 / 発話全体 = 0.54〜0.71）。

---

## 開発者向け

- 蒸留テキストの判定は `src/saanotts_jp/corpus_license.py` の 1 表だけ。
  `Verdict` は **ALLOWED / DENIED / UNKNOWN の 3 値**で、真偽値の API はありません
  （DENIED と UNKNOWN を `False` に潰すと「知らない素材」が黙って通るため）。
- ゲート: `scripts/test_corpus_license.py`（陽性対照つき）/
  `scripts/check_corpus_license.py --pack --heldout --self-test` /
  `scripts/test_accent_gate.py`（G-A1〜G-A4）。**すべて CI の python job で回ります。**
- **評価用の held-out は絞っていません**（2,333 文のまま）。評価テキストは重みに入らないので、
  絞ると v3 と比較できなくなります。`--split heldout` / `--split sibdense` では
  フィルタが**既定で切れます**。

### 訂正（この版で見つけた欠陥）

- **C-075**: `NOTICE.md` が「コーパス本文は削除済み」と書いていましたが、
  `reports/k1_measure_out.json` に Common Voice の文 33 件が残っていました。
  CI は自己テストしか回していませんでした。⚠️ 本文検出ゲートは**回せなかったときに
  `exit 2`** を返すようにしました（「0 箇所」が空虚に通るのを防ぐため）。
- **C-076**: アクセント指標の教師ゲートが生徒に依存していました（上記 4）。
- **C-077**: firmware が **C-058 で訂正済みの古い数値 `15.44%` を印字していました。**
  C-058 のとき `git grep` を `docs/` と `*.md` に限っていたためで、
  ソース 3 か所に残っていました。

全文は [`docs/decisions.md`](https://github.com/ayutaz/sanoTTS-jp/blob/main/docs/decisions.md) にあります。
