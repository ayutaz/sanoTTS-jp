# K-1 実装計画 — 端末で漢字・カタカナを扱う

作成 2026-08-31 / 前提調査 [`../research/k1-kanji-katakana-ondevice.md`](../research/k1-kanji-katakana-ondevice.md)

## 現在地（2026-08-31）

| フェーズ | 状態 |
|---|---|
| K-0 前提の凍結 | ✅ D-042（N16R8 / OTA 無し / 370,863 entries / 辞書 SHA-256 凍結） |
| K-1 ホスト側エンコーダ | ✅ blob **12,153,280 B**（全部込み。焼くのはこれ）。G1〜G5 |
| K-2 C リーダ + Viterbi | ✅ MeCab と **1,918/1,918**。G6〜G8 |
| K-3 未知語ノード生成 | ✅ MeCab と **1,977/1,977**、経路なし **0**。G9〜G11 |
| K-4 アクセント規則 4 段 | ✅ Python 版と **2,333/2,333**。G12/G13 |
| **K-4b NJD チェーンの統合** | ✅ ホストと **635/635**（NJD ノード 10,206 件）。G14a〜G14e |
| **K-5 メモリの詰め** | ✅ RAM は **1 文 104,589 B**（−61.1%）。G22〜G24。⚠️ **行列 uint8 は判断待ち** |
| **K-6 ホストとの一致** | ✅ 移植は **素性一致 244 文でラベル差 0 件**。G17/G17b/G17c |
| **K-7 ESP32 / QEMU** | ✅ 漢字文から合成まで完走。G19/G20/G21 + G25〜G27 |
| K-8 実機 | ⚠️ **次**。ボード待ち。**速度も音も未測定** |

**方針は D-043 で確定**: 端末で漢字を喋らせるところまでやる / OpenJTalk の C は
取り込んでよい / 読みの差は許容 / **動作点は K-5 の後に決める**。
⚠️ **「0.60%」は取り下げた**（C-050。同形異音語 14 文から取った数で、枝刈りの代償ではない）。

**判断が要る**: エントリ数（370,863 / 438,750 / 528,750）と、接続行列を uint8 に落とすか。
ホストとの一致（**ラベル列**・フォールバック前）は **81.67% / 85.00% / 87.33%**（M-74）。
⚠️ **ids 基準では 370,863 で 82.21%**。**定義が 3 通りあるので混ぜないこと**（M-74 の追記）。
**実装は K-7 まで完了**（QEMU で漢字文から合成まで完走。M-76）。残りは実機（K-8）。
✅ **未知語のフォールバックは入れた**（M-75）。⚠️ ただし**枝刈りの代償は減らない**（C-051）。

⚠️ **K-6 の一致率には上限がある。** ホスト既定の後処理 7 段のうち 3 段は端末に載らず
（ONNX / Sudachi / jtalk 実体）、**中間表現で 2.00%** [1.15, 3.45] の文が食い違う（M-70 / C-049）。

再現: `uv run python scripts/test_k1_dict.py` / `make -C csrc k2` / `make -C csrc k4` / `make -C csrc k4b`

---

**本計画は既存の [`phase0-1-implementation-plan.md`](phase0-1-implementation-plan.md) とは別トラック**。
あちらは「かな中間表現の生徒モデル」を実機で動かす話で、残りは実機のサイクル実測だけ。
こちらは**入力境界を漢字かな交じり文まで戻す**話で、成立すればホスト G2P が不要になる。

⚠️ **本計画は「実現可能」という調査結果に乗っているが、実機で 1 度も動かしていない。**
K-1 調査の一致率はすべて「OpenJTalk と同じ出力か」であって正しさではなく、**聴取はゼロ**。

---

## 0. 調査で既に済んでいること（やり直さない）

| 項目 | 結果 | 出所 |
|---|---|---|
| 辞書は mmap されるか | **される。98.4 MB マップ / 常駐 1,040 KB** | K-1 §4-1 |
| TTS 専用バイナリのサイズ | **1 エントリ 28.29 B**（MeCab 形式の 130 B に対し 4.6 分の 1） | K-1 §2-2 |
| 形式が解析器を駆動できるか | **held-out 全 2,333 文で 2,151/2,151 = 100.00%** | K-1 §3 |
| アクセント天井の正体 | **公開フラグを持たない 4 段 = 126 行。外部定数は 76 件のかな表のみ** | K-1 §5 |
| Viterbi / matrix.bin を落とせるか | **落とせない**（貪欲はアクセント 15.52%）。**縮むのは可** | K-1 §6-6 |
| 1 文あたりヒープ | **ピーク約 251,504 B**。支配項はラベル固定バッファ（86%） | K-1 §4 |
| カタカナのコスト | **端末テーブル 0 B** | K-1 §8 |
| MMU 窓に載るか | **N16R8 では律速にならない。フル辞書は調達可能なボードに載らない** | 本計画 §1-1 |
| 16 MB に入るエントリ数 | **OTA 無しで 370,863 / OTA2 で 291,952** | 本計画 §1-2 |

**K-0〜K-4 で実装して確かめたこと**（2026-08-31）:

| 項目 | 結果 |
|---|---|
| 辞書バイナリ（370,863 entries・**全部込み**） | **12,153,280 B = 11.59 MiB** |
| C 実装が MeCab と一致するか | **1,977 / 1,977 文**（未知語込み） |
| 経路を張れない文 | **0 / 1,977** |
| アクセント規則 4 段の C 移植 | **Python 版と 2,333 / 2,333 文一致** |
| 外部資源 | **かな表 76 件のみ**（SudachiDict 217 MB も nani ONNX も不要） |

**設計として確定したもの**（調査で測って決まった。実装で再検討しない）:

- trie の鍵は **文字 ID（idvar）**。**path 圧縮は入れない**（アルファベットを詰めた後は純損）
- **`pron` の dedup はしない**（ポインタ代で純損）。潰すのは **orig / read の例外**
- matrix.bin は **int8 行ごとスケール**（3.79 MB → 1.90 MB、アクセント句 99.74%）
- 値プールのオフセット チェックポイントは **32 間隔**
- **nbest は使わない**（Path が max 147,456 B 増える）

---

## 1. 動作点 — **実測で絞り込み済み（2026-08-31）**

計画作成時は「16 MB か 32 MB か」を未決としていたが、**3 点とも測ったので絞れた。**

### 1-1. MMU 窓 — フル辞書は現行の調達可能なボードに載らない

ESP-IDF **5.5.0** のヘッダから確定（`scripts/k1/k0_mmu_window.py`）:

| 定数 | 値 | 出所 |
|---|---|---|
| `SOC_MMU_ENTRY_NUM` | **512** | `components/soc/esp32s3/include/soc/ext_mem_defs.h:70` |
| `SOC_DRAM0_CACHE_ADDRESS_LOW/HIGH` | `0x3C000000`..`0x3E000000` = **32 MiB** | 同 `:23-24` |
| `SOC_EXTRAM_DATA_LOW/HIGH` | **同一レンジ** | `components/soc/esp32s3/include/soc/soc.h:184-185` |

**PSRAM は DBUS の窓を共有する**（`SOC_EXTRAM_DATA_*` == `SOC_DRAM0_CACHE_*`）。
B-0 §5 の主張はヘッダで裏づけられた。ボード別の flash データ用 vaddr 上限:

| ボード | PSRAM | 物理 flash | 窓 − PSRAM | 実効上限 |
|---|---:|---:|---:|---:|
| **N16R8（推奨）** | 8 MB | 16 MB | 25,165,824 | **16,777,216**（物理が律速） |
| N32R8V | 8 MB | 32 MB | **25,165,824** | 25,165,824 |
| **N32R16V（現行の 32 MB 品）** | 16 MB | 32 MB | **16,777,216** | 16,777,216 |
| PSRAM 無効 + 32 MB | 0 | 32 MB | 33,554,432 | 33,554,432 |

**結論:**

- **N16R8 では MMU は律速にならない**（窓 25.1 MB > 物理 16 MB）。効くのは
  パーティション配分だけ
- **N32R8V でも素のフル辞書 25,880,002 B は窓を 714,178 B 超える**
- **N32R16V は窓が 16 MiB に落ちる。** 最適化後のフル辞書 16,128,887 B を置くと
  残り 648,329 B で、app すら入らない → **フル辞書は不可**
- フル辞書が成立するのは **32 MB flash + PSRAM 無効**のときだけ。
  ⚠️ ただし B-0 §5 の通り **N32R8V は EOL、現行は N32R16V のみ**で調達が脆い

⚠️ **ソース読解であって実機実測ではない。** また PSRAM は全量マップされる前提で引いた。
マップ量を減らす設定があるかは未確認。

### 1-2. 16 MB ボードに入るエントリ数 — **2 回訂正した**

⚠️ **この数字は 3 世代ある。最新は最下段。**

| 世代 | 出所 | 16 MB / OTA 無しに入る entries | 何が違ったか |
|---|---|---:|---|
| K-0 の模型 | `k0_fit_point.py` | 370,863 | byte 鍵 LOUDS + 見出し語表ありの模型 |
| **C-046** | `k1_fit_point.py`（初版） | 630,000 | **辞書本体しか測っていなかった** |
| **C-047（最新）** | `k1_fit_point.py`（修正後） | **438,750** | **接続行列 3.79 MB + char.bin + unk を含めた** |

**最新の実測**（端末に置くものを全部入れた値）:

| 予算 | **入る entries** | 見出し語 | 実サイズ | 余り |
|---|---:|---:|---:|---:|
| **16 MB / A（OTA 無し）← D-042** | **438,750** | 355,768 | 13,642,192 | 185,904 |
| 16 MB / B（OTA2） | 348,750 | 283,929 | 11,657,744 | 73,200 |

**D-042 の 370,863 entries は全部込みで 12,153,280 B = 11.59 MiB**（余り 1,674,816 B）。
630,000 は **17,806,816 B で予算を 3,978,720 B 超過**する。

⚠️ **精度は測っていない。** B-0 の実測点で挟むと 263,000 → 音素 91.31% /
アクセント 81.55%、400,000 → 95.53% / 89.29%。**その間**としか言えない。
**438,750 は 400,000 を超える**ので動作点を上げる余地はあるが、**その水準の精度は未測定**。

⚠️ 接続行列を int8 行ごとスケールにすれば約 1.9 MB 浮く（K-1 §6-6）。
**K-5 の後にもう一度測り直すこと。** 今の数字は行列 int16 のまま。

### 1-3. ✅ 決定（D-042 / 2026-08-31）

**ESP32-S3-WROOM-1-N16R8 / OTA 無し / 370,863 entries（見出し語 303,501）。**
辞書予算 13,828,096 B に対し実サイズ 13,685,361 B、余り 142,735 B。

32 MB を選んでもフル辞書には届かず（§1-1）、調達だけ脆くなるため。
OTA を捨てたのは **+78,911 entries（+27%）**の効果が大きいから。

⚠️ **この動作点での精度は未実測。** K-3 の完了後に実測すること。

### 1-4. 辞書リビジョンの凍結

`scripts/k1/k0_dict_inventory.py` で全数え上げ。実体として意味があるのは 3 つ
（他 15 個は uv キャッシュ内の同一物か破損した一時ファイル）:

| 出所 | size | lexsize | SHA-256 |
|---|---:|---:|---|
| **本プロジェクト `.venv`（教師が実際に引く）** | 103,131,410 | **789,388** | `0328ca62…cef652` |
| piper-plus `build/share`（B-0 が測定に使った） | 103,082,017 | 788,923 | `a0e0f519…4bd448` |
| piper-plus `.venv` | 103,094,645 | 789,024 | `67496f05…6fd2dc` |

`matrix.bin` / `char.bin` / `unk.dic` / `left-id.def` / `right-id.def` は**全て SHA-256 一致**で、
**`sys.dic` だけが違う**（B-0 §7-4 の主張を確認）。

**✅ 決定（D-042）: `.venv` 側 `0328ca62…` を正とする。** 理由は**教師のラベル生成経路が実際に引くのがこれ**だから
（`scripts/` に `OPEN_JTALK_DICT_DIR` を設定する箇所は K-1 の測定スクリプト以外に無い）。
⚠️ **B-0 の数値は別リビジョンで測られている**ので、そのまま持ち越さないこと（C-045）。

---

## 2. フェーズ

### K-0 前提の凍結とブロッカー潰し ✅ 完了（D-042 / 2026-08-31）

**目的**: 後段が全部依存する 3 つを固定し、**唯一残っている未知のブロッカーを先に潰す**。

### ✅ 決着（D-042 / 2026-08-31）

| # | 項目 | 決定 |
|---|---|---|
| 1 | ボードと動作点 | **N16R8 / OTA 無し / 370,863 entries** |
| 2 | 辞書リビジョン | **`.venv` の `0328ca62…`**（789,388 entries。教師が実際に引く） |
| 3 | MMU 窓 | **N16R8 では律速にならない**（窓 25.1 MB > 物理 16 MB）。フル辞書は調達可能なボードに載らない |

**受け入れ条件**: ✅ **全部達成（2026-08-31）**

| # | 条件 | 結果 |
|---|---|---|
| G0-1 | ボード・エントリ数・OTA の有無が決定記録にある | ✅ **D-042** |
| G0-2 | 辞書の SHA-256 が manifest にあり、照合される | ✅ **8 ファイル一致**（`scripts/k1/dict_manifest.json`） |
| G0-3a | 陰性対照（合成）: 1 バイト違いを検出する | ✅ PASS |
| G0-3b | 陰性対照（実物）: 別リビジョンで落ちる | ✅ **2 リビジョンとも正しく落ちる** |

```bash
uv run python scripts/k1/k0_freeze_dict.py   # 凍結（既存と違えば exit 1 で止まる）
uv run python scripts/k1/k0_verify_dict.py   # 照合（G0-2 / G0-3）
```

manifest には **pyopenjtalk のバージョンも記録した**（`data/pack/manifest.json` が
記録していなかった点。C-045）。
⚠️ **K-1 のエンコーダは走る前に `k0_verify_dict.py` を通すこと。**
⚠️ G0-3b は別リビジョンの実辞書が無い環境では SKIP になる。**SKIP を「通った」と読まないこと。**

**残るリスク**: **精度が実測されていない。** 370,863 entries は B-0 の 263,000 (91.31%) と
400,000 (95.53%) の**間**としか言えず、内挿は禁止（C-009）。
K-3（未知語ノード生成）が済んだ時点で、**この動作点での精度を実測すること**。

---

### K-1 ホスト側エンコーダ ✅ 完了（2026-08-31）

**目的**: NAIST-JDIC → TTS 専用バイナリの変換器を、リポジトリに再現可能な形で置く。

**ゴール**: 選んだ動作点のエントリ集合が 1 本のバイナリになり、**全エントリを復元して
元の 11 フィールドと一致する**。

**受け入れ条件**（すべて陰性対照つき）:

| # | 条件 |
|---|---|
| G1 | 全エントリで 11 フィールド往復一致（調査では 788,923/788,923） |
| G2 | 陰性対照: レコードを 1 バイト壊すと G1 が落ちる |
| G3 | LOUDS の common-prefix-search が総当り参照と一致。**ヒット数を併記し、2 文字以上のヒットが十分あること**を示す |
| G4 | 陰性対照: **実際にヒットした経路上の**ラベルを壊すと G3 が落ちる |
| G5 | チェックポイントからのオフセット復元が materialise 版と一致 |

⚠️ **G3 は調査で 1 度空虚になった** — ランダム 3,000 語で組んだらヒットが 12,546 位置中
**17 件**しか出ず、「一致」の中身がほぼ全部「両方とも空を返した」だった。
⚠️ **G4 も 1 度空虚になった** — テスト文に現れないノードを壊しても不一致 0 になる。

**依拠する実測**: K-1 §2。**posid は (lc, rc, 品詞6つ組) から一意に決まる**ので保存不要、
`compound` 欄は全件 0、同綴りエントリは token 配列上 100% 連続。

#### ✅ 結果（2026-08-31）

実装は `src/saanotts_jp/k1_dict.py`、テストは `scripts/test_k1_dict.py`、
本番ビルドは `scripts/k1/k1_build_dict.py`。**TDD**（各スライスで RED → GREEN）。

D-042 の動作点（370,863 entries / 303,483 見出し語）:

| セクション | B |
|---|---:|
| louds | 789,862 |
| counts | 303,483 |
| classes / chains / 各表 | 74,601 |
| records (9 B 固定) | 3,337,767 |
| pool | 3,468,868 |
| **合計** | **7,967,364 B = 7.60 MiB** |

| ゲート | 結果 |
|---|---|
| **G1** 11 フィールド往復 | **370,863 / 370,863** |
| **G2** レコードを 1 B 壊す | 復元不能 → PASS |
| **G3** 総当りと一致 | 不一致 0 / 照合 5,082 位置 / ヒット 6,449 件（2 記号以上 40.1%） |
| **G4** ヒット経路を壊す | 不一致 5 件 → PASS |
| **G5** チェックポイント復元 | 不一致 0 / 標本 20,000（陰性対照つき） |

**全 789,388 entries でも通る**（G1 789,388/789,388、blob 16,727,469 B = 15.95 MiB）。

⚠️ **陰性対照 G4 / G5 は全件規模で 1 度空虚になった**（不一致 0 件）。
G4 は「ヒットを 1 件も生まない経路」のノードを壊していた、
G5 は「標本が 1 件も入っていないブロック」を壊していた。
**壊す対象が測定対象に含まれているかまで確かめないと陰性対照は空虚になる。**

⚠️ **TDD が実バグを 1 件捕まえた**: LOUDS の終端 rank は **BFS 順**であって
辞書順ではない。値の並びを辞書順に作っていたので、`lookup` が例外を出さずに
**別のエントリを返していた**。

⚠️ **この 7,967,364 B は接続行列・`char.bin`・`unk.dic` を含まない**（K-1 時点の値）。
K-2/K-3 でそれらを載せた**全部込みは 12,153,280 B = 11.59 MiB**（余り 1,674,816 B）。
⚠️ **12,102,384 B と書いてある箇所は k2 のベクタ生成時の値**。順位付けの
実装差（`run_mecab_detailed` と `run_mecab`）で選ばれる語がわずかに違う。
**端末に焼くのは `k1_build_dict.py` が出す 12,153,280 B。**
⚠️ **C-046 で「630,000 entries 入る」と報告したのは誤り**（行列を数えていなかった）。
正しくは **438,750**（C-047）。**動作点を上げるかは K-5 の後に決める**（D-043）。

---

### K-2 C リーダ + Viterbi ✅ 完了（2026-08-31）

**目的**: 端末側で辞書を引き、形態素解析する。

**ゴール**: **ホストの MeCab と held-out 全数で分割・読み・アクセントが一致する。**

**受け入れ条件**:

| # | 条件 |
|---|---|
| G6 | held-out 全 2,333 文で MeCab と分割・`pron`・(acc, chain) が一致（未知語を含む文を除く） |
| G7 | 陰性対照: 接続コストを 0 にすると G6 が落ちる |
| G8 | `cc -Wall -Wextra` で警告 0、malloc をコアで呼ばない（arena を渡す。既存 C99 コアと同じ規約） |

**依拠する実測**: K-1 §3 でホスト実装が 2,151/2,151 = 100.00% を達成済み。**同じ突き合わせを C で再現する。**

⚠️ **実装で必ず踏む落とし穴**（調査で全部踏んだ。K-1 §9-3 / §9-4）:

- **matrix.bin の索引は MeCab 本家の式では合わない。** 正しくは
  `flat[rc_prev + 1377 * lc_cur]`（本家の `lcAttr + lsize*rcAttr` は一致 0.91%）
- **LOUDS の子ノード番号は `rank1[p]`**（`rank1[p+1]` ではない）。誤ると
  **落ちずに「全部 1 文字に分割される」**
- **位置ごとに 1 本だけ最良経路を残すのは Viterbi ではない。** ノードごとに前任を持つ
- `link_cost` は `word_cost` を含む。生の遷移コストと比べると**陰性対照が壊れる**

#### ✅ 結果（2026-08-31）

`csrc/k1dict.{h,c}` / `csrc/k2_test.c` / `make -C csrc k2`。**TDD**（ヘッダとゲートを
先に書き、スタブで RED を確認してから実装）。

**D-042 の動作点（370,863 entries / 303,483 見出し語）:**

| ゲート | 結果 |
|---|---|
| **G6** MeCab と一致 | **1,918 / 1,918 文**（token 31,412 件） |
| **G7** 接続コスト 0 で落ちる | 1,805 / 1,918 文 |
| **G8** 警告 0 / malloc なし | `cc -Wall -Wextra` 0 行、alloc 系シンボルなし |

除外は 415 文（未知語 78 = K-3 の担当 / 枝刈りで欠け 337）。

⚠️ **ゲートが実バグを 1 件捕まえた。** 最初 174/177 で、残り 3 件は entry index が
1 つずれるだけだった。同点ではなく**実害**で、白熊（シロクマ / ハグマ）や
漁り（アサリ / スナドリ）が **lc/rc/wcost が同一なのに read/pron/acc が違う**。
Viterbi のコストでは決着せず、MeCab は辞書順で先勝ちする。
私の連結リストが生成と逆順だったので同点時に後の候補が勝っていた。
**同点はノード番号が小さい方**と明示して修正した。

**blob に足したもの**（K-1 から。どちらも TDD）:

- **rank/select 索引** — 端末は flash から mmap するので起動時に索引を作ると RAM を食う。
  superblock は **256 bit**（512 bit だと先行 1 が最大 448 で u8 に入らない）。実測最大 191
- **surfck** — 見出し語 rank → エントリ開始位置（32 見出し語おき）
- **matrix** — 接続行列。索引は `flat[rc_prev + lsize*lc_cur]`

⚠️ **半角→全角正規化は K-2 の対象外。** OpenJTalk は MeCab の前にこれを掛けるので、
ベクタの参照テキストは「MeCab が実際に見た列」にしてある。**端末側の前処理として別途要る。**
⚠️ ベクタの座標は**鍵バイト**。UTF-8 バイトにすると「解析は正しいのに全件不一致」に見える。

---

### K-3 未知語ノード生成 ✅ 完了（2026-08-31）

**目的**: 辞書に無い語で経路が途切れないようにする。

**ゴール**: **経路を張れない文が 0 件**になる。

**受け入れ条件**:

| # | 条件 |
|---|---|
| G9 | held-out 全数で経路なしが 0 件（調査のホスト実装では 104 文 = 4.5% が張れなかった） |
| G10 | 未知語を含む 78 文で MeCab と出力が一致 |
| G11 | 陽性対照: 幽霊漢字（彁 / 妛 / 椦）で**意図どおり脱落する**ことを確認 |

**依拠する実測**: 未知語は held-out 全数で **83 / 38,269 token = 0.22%**、**78 文 = 3.34%**。
中身は**ほぼカタカナの外国固有名詞の断片**（`ーヌ` `ーヴァ` `ーリャ`）と単漢字。
`unk.dic` の feature は **7 列しかなく `read` / `pron` / `acc` / `chain` を持たない**
（これが「無音で消える」の正体）。

⚠️ **未知語は誤読ではなく無音で消える。** 音では気づけないので、**脱落文字数を数えるゲート**
を必ず置くこと。

#### ✅ 結果（2026-08-31）

`csrc/k1dict.c` に実装、`make -C csrc k2` で G6〜G11 をまとめて回す。

**D-042 の動作点（370,863 entries / held-out 1,977 文）:**

| ゲート | 結果 |
|---|---|
| **G6** MeCab と一致 | **1,977 / 1,977 文**（token 32,386 件） |
| **G9** 経路を張れない文 | **0 / 1,977** |
| **G10** 未知語を含む文で一致 | **59 / 59** |
| **G11** 陽性対照（幽霊漢字） | 未知語ノードになる |

K-2 では 415 文を除外していたが、未知語を扱えるようになり除外は
**枝刈りで欠けた 356 文だけ**になった。

**規則は実データから逆算した**（記憶で書かない）:

- **wcost は `unk.dic` の値をそのまま使う**（8360 / 10654 が MeCab の申告と一致。スケーリング無し）
- `char.bin` は u32 カテゴリ数 11 / 32 B ずつの名前 / **65,535** 件の CharInfo。
  **U+FFFF 以上は表に無い** — サロゲートペア漢字が未知語になる理由（C-044）
- CharInfo は `type:18 / default_type:8 / length:4 / group:1 / invoke:1`

⚠️ **ゲートが実バグを 2 件捕まえた:**

1. **length ノードが category をまたいでいた。**「たけーな」で `ー`(KATAKANA, length=2) が
   次の `な`(HIRAGANA) を巻き込んでいた。**MeCab は length ノードも同カテゴリ内に限る。**
2. **G10 の分母が失敗ケースを除外していた。**「53/53 通過」と表示しながら実際は 59 件中
   6 件落ちていた。**分母は先に数える。**

---

### K-4 アクセント規則の C 移植 ✅ 完了（2026-08-31）← **新規性の中核**

**目的**: 「C 実装だけではアクセントが 76% で頭打ち」という制約を外す。

**ゴール**: **既定経路とアクセントが 100.00% 一致する。**

**移植対象は 4 関数・126 行**（すべて NJD ノード列だけを見る純関数）:

| 段 | 実質行数 | 外部定数 | LOO 不一致文数 |
|---|---:|---|---:|
| `retreat_acc_nuc` | 36 | **なし** | **214** |
| `modify_acc_after_chaining` | 42 | **なし** | **204** |
| `modify_filler_accent` | 21 | なし | 84 |
| `suppress_u_long` | 27 | `_DAN_MAP` 76 件（かな表） | 0 |

**受け入れ条件**:

| # | 条件 |
|---|---|
| G12 | held-out 全数で、既定経路とアクセント（chain_flag 連結の句列）が一致 |
| G13 | 陰性対照: 4 段のうち 1 段を抜くと G12 が落ちる（各段について） |

**依拠する実測**: K-1 §5。**語彙 2 段を切ってもアクセントは 100.00% 一致する**
（SudachiDict 217 MB と nani ONNX の寄与は合計 0.13pt）。参照実装は `g2p_pure4.py`。

⚠️ `process_odori_features` は**移植しない**（`jtalk.run_frontend()` に再入する不純な段で、
寄与は 0 文）。

⚠️ **読みの差 0.60% は残る。** これは語彙側（SudachiDict）なので安くは直らない。
ただし**ラベル生成を端末と同じ設定に揃えれば定義上消える**（K-6 の判断事項）。

#### ✅ 結果（2026-08-31）

`csrc/k4_accent.{h,c}` / `csrc/k4_test.c` / `make -C csrc k4`。

| ゲート | 結果 |
|---|---|
| **G12** 4 段の適用が Python 版と一致 | **2,333 / 2,333 文**（held-out 全数） |
| **G13** 段ごとの陰性対照 | 判別力のある段 **3 / 4** |

段別の「抜くと結果が変わる」文数 = **85 / 0 / 218 / 204**。
K-1 調査の LOO 実測（84 / 0 / 214 / 204）と**独立に一致した**。

**外部資源は `_DAN_MAP` 76 件（かな → 母音）だけ。**
SudachiDict 217 MB も nani ONNX も要らない。

**ゲートが本当に落ちるかを確かめた**（実装にバグを注入）:

| 注入 | 結果 |
|---|---|
| retreat の符号を反転 | NG 538/600 → 捕まえた |
| マス形の 1/2 を入れ替え | NG 563/600 → 捕まえた |
| filler の `>` を `>=` に | NG 549/600 → 捕まえた |
| **4 段の順序を入れ替え** | **OK 600/600 → 捕まえられない** |

⚠️ **「順序は入れ替えてはいけない」は未検証だった。** Python 側で測ると
retreat と chaining の入れ替えは held-out 1,200 文で **1 文も変わらない**。
ゲートが弱いのではなく **corpus が段の順序を区別しない**。

⚠️ `suppress_u_long` はこの corpus で **1 文も動かさない**（LOO 0）。
その段の陰性対照は**空虚**なので PASS ではなく「効かない」と表示する。

⚠️ **これは 4 段の移植だけ。** NJD チェーン本体（`mecab2njd` / `njd_set_*`）は
まだ統合していないので、**端末で end-to-end にアクセントが一致するかは未確認**。

---

### K-4b NJD チェーンの統合 ← **計画に無かった。K-4 で判明** ✅ **完了（2026-08-31）**

**目的**: MeCab の出力から K-4 の 4 段へ渡せる NJD ノード列を、端末上で作る。

⚠️ **当初の計画はこの段を持っていなかった。** K-4 は「4 段 = 126 行」だけを
移植対象にしていたが、**その 4 段は NJD ノード列を入力に取る**。
MeCab の出力（形態素）から NJD ノード列を作るのは別の処理で、
OpenJTalk の C 実装（`mecab2njd` + `njd_set_*` 群 + `jpcommon`）が担っている。

**結果（M-69）**: `make -C csrc k4b` が **635 / 635 文・NJD ノード 10,206 件**で
ホストと一致する。取り込みは **34 ファイル**（`csrc/openjtalk/`）、
自分で書いた C は `csrc/k4b_njd.c` の **227 行**だけ。

**受け入れ条件**:

| # | 条件 | 結果 |
|---|---|---|
| G14a | held-out で NJD ノード列（11 文字列 + `acc` / `mora_size` / `chain_flag`）がホストと一致 | ✅ **635 / 635** |
| G14b | 陰性対照: 段を 1 つ抜くと G14a が落ちる | ✅ **6 / 7**（`njd_set_long_vowel` は上流が no-op） |
| G14c | ライセンス（修正 BSD。`COPYING`。**GPL の `sanoTTS` とは別物**） | ✅ |
| G14d | 取り込んだ第三者コードの出所とバージョンを記録する | ✅ `PROVENANCE.md`（生成物） |
| G14e | **chaining 前の 12 規則を 1 つずつ抜いて落ちるか** | ⚠️ **11 / 12**（下記） |

#### 分かったこと 3 つ

**1. 取り込み元は `pyopenjtalk` ではなく `pyopenjtalk-plus`**（C-048）。
素の方から取ると **34 ファイル中 22 が食い違い**、G14a は **75 / 600** で止まる。
`njd_set_accent_phrase.c` にはフォークが足した Rule 13 などが入っている。

```bash
uv run python scripts/k1/k4b_vendor.py --sdist pyopenjtalk_plus-<ver>.tar.gz
uv run python scripts/k1/k4b_vendor.py --sdist <同> --check   # バイト一致の検査
```

**2. 素の Open JTalk に無い段が chaining の前にもう 1 つあった。**
`openjtalk.pyx` の `apply_original_rule_before_chaining`（12 規則）で、
`njd_set_pronunciation` と `njd_set_digit` の**間**に挟まる。
移植前は **302 / 600** で止まっていた。`csrc/k4b_njd.c` に写した。

```
mecab2njd → njd_set_pronunciation → **before_chaining** → njd_set_digit
→ njd_set_accent_phrase → njd_set_accent_type → njd_set_unvoiced_vowel
→ njd_set_long_vowel
```

**3. 端末に載らない後処理が 3 段ある**（C-049 / M-70）。ホスト既定は 7 段だが、
K-4 が移植したのは純関数の 4 段だけ。`predict_nani_reading` は **ONNX モデル**、
`modify_kanji_yomi` は **Sudachi**、`process_odori_features` は jtalk 実体を要求する。
**中間表現で食い違うのは 12 / 600 = 2.00%** [1.15, 3.45]、**全部 Sudachi 由来**で、
症状は**無声化マーク `°` の有無**。⚠️ **音への影響は測っていない。**

#### ⚠️ ゲートが見ていないもの

- **規則 3（数+分+の+数→ブン）は検証できていない。** 635 文で 5 回発火するが、
  書いた read/pron を後段の `njd_set_digit` が必ず上書きする。
  **出力を `"ズズズ"` に変えても G14a は 635 / 635 のまま通る。**
  文を 39 通り試したが効く例は無かった
- **`njd_set_long_vowel` は上流が `#if 1 return;` の no-op。** 0 / 635 は
  corpus が弱いのではない
- **速度もメモリも測っていない**（K-5）。**実機でも動かしていない**

#### 取り込んだもの

| モジュール | ファイル |
|---|---:|
| `text2mecab` / `mecab2njd` / `njd` | 8 |
| `njd_set_pronunciation` / **`njd_set_digit`** / `njd_set_accent_phrase` | 9 |
| `njd_set_accent_type` / `njd_set_unvoiced_vowel` / `njd_set_long_vowel` | 9 |
| `njd2jpcommon` / `jpcommon` | 8 |
| **合計** | **34**（+ `COPYING`） |

⚠️ **素で数えると 20,580 行**（79 ファイル）だが、その 61% は
**文字コード別のヘッダ**（`_shift_jis` / `_euc_jp` / `_ascii_for_*`）。
UTF-8 だけ残せば **8,070 行**（pyopenjtalk 0.4.1 で数えた値。フォークでは微増）。

**ライセンス**: 修正 BSD（`COPYING`。Nagoya Institute of Technology /
HTS Working Group, 2008-2016）。MIT との同居に問題はない。
⚠️ **GPL-3.0 の `Ampixa/sanoTTS` とは別物**（D-032 の凍結対象ではない）。

⚠️ **取り込んだコードは改変しない。** 改変すると「ホストと一致するか」の
基準が自分の改変に依存してしまう。詰めるのは K-5（ラベルバッファ 1024 → 256）で、
**一致を確認したあとにやる**。`--check` がバイト一致を守る。

### K-5 メモリの詰め ✅ **RAM 側は完了（2026-08-31）**

**目的**: 1 文あたりのピーク RAM を予算内に収める。

**ゴール**: **1 文ピークが実測で 150 KB 以下** → ✅ **104,589 B**（M-71）。

**受け入れ条件**:

| # | 条件 | 結果 |
|---|---|---|
| G22 | 最長文でのヒープ + スタックのピークが実測されている | ✅ ヒープ 97,325 B + スタック 7,264 B |
| G23 | ラベルバッファの固定長を詰めてある | ✅ `MAXBUFLEN` **1024 → 256** |
| G24 | 陰性対照: 詰める前後で出力が bit 一致 | ✅ **ホストと 34,997 本一致**（陽性対照つき） |

⚠️ **番号を G14/G15/G16 から振り直した。** K-4b が G14a〜G14e を取ったので
「G14」が 2 つの別物を指していた。G15〜G17 は K-6 が使っている。

再現: `make -C csrc k5`

| | 詰める前 | **詰めた後** |
|---|---:|---:|
| 1 文ピーク（最長文 100 文） | 268,941 B | **104,589 B**（−61.1%） |
| ラベルバッファの占める分 | 219,136 B（83.7%） | 54,784 B（56.3%） |

⚠️ **取り込んだ C を測るために改変してはいない。** ヒープは
`cc -include k5_alloc.h` でマクロ差し替え、スタックは自前スタックを
0xA5 で塗って pthread で走らせ塗り残しを数えた。
`MAXBUFLEN` の変更だけは `k4b_vendor.py` の `PATCHES` に**理由つきで**登録してあり、
`--check` が「上流 + PATCHES」と突き合わせる。**表に無い改変は落ちる。**

⚠️ **ホストでの実測であって ESP32 の値ではない。**
⚠️ **K-2 / K-3 の辞書引きとラティスは含まない**（別の測定）。

#### 残り: 接続行列を 1 バイトに落とすか（**判断待ち**）

測った（M-72）。**タダではない**:

| | 生の int16 | **行ごとアフィン uint8** |
|---|---:|---:|
| `matrix.bin` | 3,792,262 B | 1,907,149 B（**−1,885,113**） |
| MeCab との一致（n=1,696） | 1,696 / 1,696 | 1,693 / 1,696（**−0.18%** [0.06, 0.52]） |
| 入る entries（OTA 無し） | 438,750 | **528,750**（+20.5%） |
| 全語が辞書にある文（n=2,000） | 89.95% | **91.60%**（**+1.65 pt**） |

**取引: 文カバー率 +1.65 pt ↔ MeCab 一致 −0.18 pt。**
⚠️ **未知語は誤読ではなく無音で消える**（B-0）ので、カバー率の方が重い可能性が高い。
⚠️ **最終的な音素精度・アクセント精度は未測定。** カバー率は
「辞書に語がある」であって「読みが正しい」ではない。
⚠️ **C 側の実装はまだ無い。** 測ったのは「int16 のまま値だけ丸めた」ブロブ。
採るなら形式を変えて**再測が要る**。
⚠️ 行ごと**対称** int8 は同じサイズで **0.53%** 落ちる。**採るならアフィン。**
⚠️ 無損失の行パレットは効かない（行あたりの異なり値が中央 761 で **105.2%** に増える）。

### K-6 ホストとの一致 ✅ **移植は完了（2026-08-31）**

**目的**: 端末が出す音素列が、生徒が学習した入力と一致することを保証する。

**結果（M-74）**: `make -C csrc k6` が
**素性が一致した 244 文で、ラベルの食い違い 0 件**（陰性対照 44 件）。
端末の全段を 1 本に繋いだ経路:

```
漢字文 → k1_encode_key → k1_analyze（K-2/K-3）→ k1_entry_feature
       → mecab2njd → 8 段（K-4b）→ k4_apply（K-4 の 4 段）
       → njd2jpcommon → JPCommon_make_label
```

**受け入れ条件**:

| # | 条件 | 結果 |
|---|---|---|
| G17 | **素性が一致した文でラベルが一致する**（移植の正しさ） | ✅ **0 件差 / 244 文**（陰性対照 44 件） |
| G17b | 辞書の枝刈りの代償を**数として出す**（閾値は置かない） | ✅ 12.67〜18.33% |
| G17c | 参考: ホスト既定（Sudachi / ONNX 込み）との差 | ✅ 80.00% 一致 |
| G18 | 既存の入力仕様が壊れていない（`make -C csrc all-test`） | ✅ |

⚠️ **G17 の受け入れ条件を書き換えた**（C-050）。元は
「食い違いが 0.60%（14 文）以下」だったが、**14 文は同形異音語（Sudachi）の数**で
辞書の枝刈りの代償ではなかった。実測は **12.67〜18.33%** で 1 桁違う。
**しきい値は実測に合わせて置き直していない** — 動作点が決まっていないのに
実測に合わせて書くと、それは回帰検出であって合格基準ではないため。

#### この段で足したもの

| | |
|---|---|
| `poolck` セクション | 値プールの索引。無いと entry のフィールドを引くのに **O(n) 走査**（TDD で追加） |
| `termck` セクション | 終端ランクの索引。`term_rank()` の O(n) を潰し、**rank → ノード**の逆引きも賄う |
| `k1_key_to_utf8` | 鍵の区間 → 表層形 |
| `k1_entry_feature` | エントリ → **MeCab feature 文字列**（`mecab2njd` にそのまま渡せる） |
| `k1_unk_feature` | 未知語の 8 列 feature |
| `k1_surface_of_rank` | LOUDS を親へ遡って見出し語を復元（`orig` が ID で入っているため） |

blob の version は **2**（C 側は 1 も読める）。

#### 残っていること

- **エントリ数の決定**（M-72 / M-73 / M-74）。370,863 → 528,750 で一致 **+5.66 pt**
- ✅ **未知語のフォールバック**（`k1_unk_guess`。M-75）。未知語 37 件のうち
  **13 件**が読めるようになった。⚠️ **枝刈りの代償は 1 mm も減らない** —
  落ちた語は未知語にならず**切り直されて誤読される**（C-051）
- **音は一度も聞いていない。実機でも動かしていない。**

### K-7 ESP32 ビルドと QEMU ✅ **完了（2026-08-31）**

**目的**: 実機に持っていく前に、載ることと動くことを確かめる。

**結果（M-76）**: **QEMU の UART に漢字文を打ち込み、合成まで完走した。**

```
かな> !今日は良い天気ですね。
I (21915) saanotts: 形態素 7 個 / ids 53 個
I (23072) saanotts: 出力 PCM: 27136 sample / FNV-1a 0x78c209af06affc01
```

**受け入れ条件**:

| # | 条件 | 結果 |
|---|---|---|
| G19 | `idf.py build` が通り、app 枠と辞書パーティションに収まる | ✅ app **359,584 B / 2 MB**（17.1%）/ dict **12,153,280 B / 13,828,096 B**（87.9%） |
| G20 | QEMU で漢字文を 1 行入力して合成が完走する | ✅ 形態素 7 → ids 53 → 27,136 sample |
| G21 | **同じターゲット上の 2 構成**で既存経路の出力が bit 一致 | ✅ **3 経路すべて `0x78c209af06affc01`** |
| G25〜G27 | ラベル → 生徒インデックス（ホスト側。`make -C csrc k7`） | ✅ **298/298**（陰性対照 298 件） |

#### G21 は要求より強い結果になった

| 構成 | 入力 | 出力 PCM の FNV-1a |
|---|---|---|
| 漢字あり | `!今日は良い天気ですね。` | **0x78c209af06affc01** |
| 漢字あり | `きょ][おわよ][いて][んきです°ね` | **0x78c209af06affc01** |
| 漢字なし | `きょ][おわよ][いて][んきです°ね` | **0x78c209af06affc01** |

**端末が自分で解析した漢字文が、凍結してあるかな中間表現と同じ ids 列になった。**
⚠️ **1 文だけの一致。** ホストと同じ ids になるのは 82.21% の文（M-74 の追記）。
⚠️ 陰性対照: 辞書なしビルドに `!今日は…` を打つと**正しく拒否される**。

#### この段で足したもの

| | |
|---|---|
| `esp32/partitions_16mb.csv` | 16 MB / OTA 無し。**dict の 13,828,096 B は D-042 の予算と一致** |
| `esp32/sdkconfig.kanji` | 16 MB flash + 表の差し替え |
| `esp32/main/saan_dict.c` | dict パーティションの mmap（64 KB 境界を検査） |
| `esp32/main/saan_kanji.c` | 漢字文 → ids（端末の全段） |
| `csrc/k7_label2ids.c` | **ラベル → 生徒インデックス**（`_phonemize_core` の移植） |
| `csrc/k7_table.h` / `k7_dan.h` | 語彙 57 / `_DAN_MAP` 76（どちらも**機械生成**） |

有効化は **`idf.py -DSAAN_KANJI=1`** の 1 本。既定は無効（かな入力だけの v0.1.1 のまま）。

#### ⚠️ DRAM は 2 回溢れた

(1) 作業領域を `.bss` に静的確保 → **419 KB 超過**。
(2) heap から取ろうとした → **空きが 20,964 B しか無い**。

**大きい 2 つ（feat 30,720 B / k4 ノード 50,304 B）を合成用 arena から
切り出して解決した** — G2P と合成は同時に走らない。
⚠️ **PSRAM は使えなかった** — QEMU が octal PSRAM を持っていない
（`PSRAM ID read error` → abort）。`kj_alloc` は PSRAM 優先のままなので
実機では有効にできる。

#### 残っていること

- **速度は測れていない。** QEMU の `xRT 0.661` は**使えない数字**
  （サイクル精度ではない）。**実機が唯一の残り**（K-8）
- **音は一度も聞いていない**
- エントリ数と接続行列の判断（M-72 / M-74）は未決のまま

### K-8 実機

**目的**: 唯一測れていないものを測る。

**ゴール**: **1 文あたりの G2P レイテンシがサイクル実測で出ている。**

**受け入れ条件**: G2P が発話長に対して十分小さい（目安 1% 未満）ことが実測で示される。

⚠️ **ボードが無い。** これは既存計画の D-3d と同じ待ちで、K-1 固有の障害ではない。

---

## 3. 主要リスク

| リスク | 影響 | 状態 |
|---|---|---|
| ~~ESP32-S3 の mmap 窓にフル辞書が入らない~~ | ~~32 MB 案が消える~~ | ✅ **確認した**（§1-1）。ESP-IDF 5.5.0 のヘッダで窓 32 MiB を PSRAM と共有。**フル辞書は N32R8V でも 714,178 B 超過、N32R16V は窓が 16 MiB に落ちて不可**。16 MB 案は影響なし。⚠️ ソース読解であって実機実測ではない |
| **実機が無い** | レイテンシが測れない | ⚠️ 既存計画 D-3d と同じ待ち |
| **スタック使用量が未計測** | K-5 のゴールが立たない | ⚠️ `jpcommon_label.c` 等がフレームに `char buff[1024]` を持つ |
| **OpenJTalk C 本体のコードサイズ** | app 枠に入らない可能性 | ⚠️ **xtensa ビルド未実施**。B-0 の時点から未測定のまま |
| ~~126 行の移植忠実度~~ | ~~アクセントが戻らない~~ | ✅ **解決**（K-4）。held-out 全 2,333 文で Python 版と一致。実装にバグを 4 種注入して 3 種を捕まえた。⚠️ 段の順序だけは corpus が区別しない |
| **一致 ≠ 正しさ** | 「教師と同じ」だが日本語として正しいかは別 | ⚠️ ネイティブ正解データが存在しない。**聴取ゼロ** |
| 未知語がカタカナ外国名に集中 | 固有名詞の多い文で脱落 | ⚠️ 0.22% token / 3.34% 文。**ドメイン外プローブが 31 文しかない** |
| ヒープ実測が macOS の malloc | ESP32 に持ち越せない | ⚠️ 持ち越せるのは `sizeof` のクロスコンパイル値だけ |
| ~~辞書リビジョンの取り違え~~ | ~~測定が別物になる~~ | ✅ **凍結した**（D-042）。`scripts/k1/dict_manifest.json` に 8 ファイルの SHA-256、`k0_verify_dict.py` が照合、hook が破棄を deny する |
| **NJD チェーンが未統合** | **K-6 以降が着手できない** | ⚠️ **計画に無かった段**（K-4b として追加）。移植ではなく vendoring が現実的だが、**作業量は未測定** |
| **サイズの測り方を 2 回間違えた** | 動作点の判断を誤る | ⚠️ C-046（辞書本体しか測らず 630,000 と報告）→ C-047（全部込みで 438,750）。**K-5 の後にもう一度測り直す** |

---

## 4. 測っていないこと

- **実機で 1 度も動かしていない。** 速度は誰も測っていない
- **聴取ゼロ。** 核位置が 4 モーラずれる 47 件が可聴かは不明
- **枝刈り水準での精度は新形式で測り直していない**（フル辞書での転移は構成的に成立）
- **LOUDS と darts のページ数比較は未成立**（3 者が別々の領域を数えていた。K-1 §4-2）
- **合成した最適化イメージ（15.38 MiB）は算術**。まとめてビルドしていない
- **外部の先行事例（AquesTalk 等）は未調査**（調査時に WebSearch が拒否された）
- **動作点（370,863 entries）での精度**。B-0 の 263,000（91.31%）と 400,000（95.53%）の
  間としか言えない。K-2/K-3 のベクタが「枝刈りで欠け 356 文」として落としているのが
  まさにその劣化で、**測るには参照をフル辞書側で取る別のゲートが要る**
- **NJD チェーンの作業量**（K-4b）。vendoring する C の行数もライセンス整理も未着手
- **アクセント規則 4 段の end-to-end 効果**。4 段が Python と一致することは示したが、
  **NJD が無いので端末で本当にアクセントが戻るかは未確認**

---

## 5. やらないこと

- **貪欲最長一致で Viterbi を置き換える**（アクセント 15.52% で使い物にならない）
- **path 圧縮**（文字 ID 鍵の後は純損）
- **`pron` の dedup**（ポインタ代で純損）
- **nbest API**（Path が max 147,456 B 増える）
- **`process_odori_features` の移植**（不純・寄与 0）
- **かな中間表現の入力仕様を捨てる**（K-6 G18 で守る。漢字対応は**追加**であって置換ではない）
- **ラベルの再生成**（D-043。読みの差 0.60% は許容する。D-015 に触れない）
- **`njd_set_*` を自前で書き直す**（K-4b は vendoring。8,070 行を書き直す価値は無い）
- **文字コード別のヘッダを取り込む**（`_shift_jis` / `_euc_jp` / `_ascii_for_*` = 12,510 行。UTF-8 だけでよい）
